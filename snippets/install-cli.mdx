Run directly with `npx` (no install needed):

```bash
npx @mandate/cli login --name "my-agent"
```

Or install globally:

<Tabs>
  <Tab title="bun">
    ```bash
    bun add -g @mandate/cli
    ```
  </Tab>
  <Tab title="npm">
    ```bash
    npm install -g @mandate/cli
    ```
  </Tab>
</Tabs>
